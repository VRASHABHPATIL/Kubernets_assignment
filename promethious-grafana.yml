grafana:
  adminPassword: "admin"
  service:
    type: LoadBalancer   
prometheus:
  prometheusSpec:
    serviceMonitorSelectorNilUsesHelmValues: false
    retention: 15d       
    resources:
      requests:
        memory: 400Mi
        cpu: 200m
kubeStateMetrics:
  enabled: true
nodeExporter:
  enabled: true